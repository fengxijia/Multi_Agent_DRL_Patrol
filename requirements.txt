# Modern stack (recommended) -- Python >= 3.9.
# Install with:  pip install -r requirements.txt  (or: pip install -e .)
#
# numpy is pinned < 2.0 and networkx < 3.0 because the environment uses
# np.matrix / .A and the networkx 2.x adjacency-matrix API.
numpy>=1.21,<2.0
torch>=2.1
gymnasium>=0.29
networkx>=2.6,<3.0
matplotlib>=3.5
scikit-learn>=1.1
imageio>=2.19
tensorboard>=2.11
